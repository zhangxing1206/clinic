<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.dao.EmployeeMapper">
    <select id="employeeInfo" resultType="cn.pojo.Employee">
        select * from  `employee` where `eSection`=#{eSection}
    </select>

</mapper>